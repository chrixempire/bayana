# Bayana API (copy to `.env.local` and adjust for your backend)
#
# Dev default: requests go to `/api`, proxied by Vite to VITE_API_PROXY_TARGET.
# Production: set an absolute API URL.
# VITE_API_BASE_URL=https://api.joinbayana.com

VITE_API_PROXY_TARGET=http://localhost:8080

# Frontend URL for email verification links (ask backend to use this instead of the API URL).
# Required for Google OAuth: passed as redirect_uri when starting sign-in so the API can
# redirect to /auth/google/callback?token= after Google approves access.
# Production example: VITE_APP_URL=https://joinbayana.com
VITE_APP_URL=http://localhost:5173

# When true, pages should call authApi / onboardingApi / orgApi instead of mocks.
VITE_API_INTEGRATION_ENABLED=false

# HttpOnly cookie auth (requires backend support — see below).
# When true: login/register responses must Set-Cookie; the app will NOT store tokens in localStorage.
# VITE_AUTH_COOKIE=true
#
# Backend requirements for VITE_AUTH_COOKIE=true:
# - Set HttpOnly, Secure, SameSite=Lax (or None+Secure cross-site) session cookie on register/login/logout
# - CORS: Access-Control-Allow-Credentials: true + explicit Allow-Origin (not *)
# - Accept cookie auth on protected routes (not only Authorization: Bearer)
# - Optional: stop returning data.token in JSON once cookies are live
