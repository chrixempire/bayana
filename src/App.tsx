export default function App() {
  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-2 text-center">
      <h1 className="text-2xl font-semibold">Bayana Frontend</h1>
      <p className="text-sm opacity-70">
        Framework baseline — feature implementation is merged in from feature branches.
      </p>
    </main>
  )
}
